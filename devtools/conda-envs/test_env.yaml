name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - hsd-python
  - seamm
  - seamm-util
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    - cms_plots
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike
