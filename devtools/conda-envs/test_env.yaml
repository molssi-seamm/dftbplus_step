name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - openbabel
  - seamm

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - pygments
    - sphinxnotes-strike
    - sphinx-rtd-theme
    - pystemmer
