name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - hsd-python
  - seamm
  - seamm-util
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - cms_plots
    # Documentation
    - rinohtype
    - pygments
    - sphinxnotes-strike
    - sphinx-rtd-theme
    - pystemmer
