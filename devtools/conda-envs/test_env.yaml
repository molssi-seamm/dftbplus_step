name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - openbabel

  # Testing
  - flake8
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - seamm
    - seamm-util
    - seamm-widgets

    # Testing
    - yapf

